"use client"
import Link from "next/link"
import { useRouter } from "next/navigation"
import { useState } from "react"
import type { Connection } from "@/lib/afd/contracts"
import { formatEnum } from "@/lib/afd/display"
import { mutateAfd } from "@/lib/afd/browser-client"
import { Button } from "@/components/ui/button"
import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"
import { AlertDialog,AlertDialogAction,AlertDialogCancel,AlertDialogContent,AlertDialogDescription,AlertDialogFooter,AlertDialogHeader,AlertDialogTitle } from "@/components/ui/alert-dialog"
import { StatusPill } from "@/components/desk/status-pill"
import { EmptyState } from "@/components/desk/page-header"
export function BrokerSettings({connections}:{connections:Connection[]}){const router=useRouter();const [busy,setBusy]=useState("");const [remove,setRemove]=useState<Connection|null>(null);const [status,setStatus]=useState("");async function act(path:string,method:"POST"|"PUT"|"DELETE"){setBusy(path);setStatus("Saving…");try{await mutateAfd(path,method);setStatus("Change confirmed.");setRemove(null);router.refresh()}catch{setStatus("Change failed. The previous connection state remains active.")}finally{setBusy("")}}return <div className="space-y-4">{status&&<p role="status" className="rounded border p-3 text-sm">{status}</p>}<div className="flex justify-end"><Button asChild><Link href="/connect-tradelocker?new=1">Add Trading Connection</Link></Button></div>{connections.map(connection=><Card key={connection.public_id}><CardHeader><div className="flex justify-between"><div><CardTitle>{connection.broker_name||connection.server||"HeroFX"}</CardTitle><p className="text-sm text-muted-foreground">Connected through {connection.platform_name||"TradeLocker"}</p></div><StatusPill label={formatEnum(connection.environment)} tone={connection.environment==="live"?"bad":"good"}/></div></CardHeader><CardContent><dl className="grid gap-3 text-sm sm:grid-cols-3"><Datum label="Status" value={connection.enabled?"Connected":"Disabled or reauthentication required"}/><Datum label="Discovered accounts" value={String(connection.account_count)}/><Datum label="Last verified" value={connection.last_verified_at?new Date(connection.last_verified_at).toLocaleString():"Not recorded"}/></dl><div className="mt-5 flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={()=>void act(`broker/tradelocker/discover-accounts?connection_id=${encodeURIComponent(connection.public_id)}`,"POST")}>Refresh Accounts</Button><Button asChild size="sm" variant="outline"><Link href={`/connect-tradelocker?connection_id=${encodeURIComponent(connection.public_id)}`}>Reauthenticate</Link></Button><Button size="sm" variant="outline" disabled={!connection.enabled||Boolean(busy)} onClick={()=>void act(`broker/connections/${connection.public_id}/disable`,"PUT")}>Disable</Button><Button size="sm" variant="ghost" className="text-red-600" onClick={()=>setRemove(connection)}>Remove Connection</Button></div></CardContent></Card>)}{!connections.length&&<EmptyState title="No trading connections" description="Add TradeLocker to discover HeroFX accounts." action={<Button asChild><Link href="/connect-tradelocker">Add Trading Connection</Link></Button>}/>}<AlertDialog open={Boolean(remove)} onOpenChange={open=>!open&&setRemove(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Remove {remove?.broker_name||remove?.server} connection?</AlertDialogTitle><AlertDialogDescription>This removes stored broker credentials when no strategies remain bound. This action cannot be undone.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction disabled={Boolean(busy)} onClick={()=>remove&&void act(`broker/connections/${remove.public_id}`,"DELETE")}>Remove Connection</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></div>}
function Datum({label,value}:{label:string;value:string}){return <div><dt className="text-xs text-muted-foreground">{label}</dt><dd className="mt-1">{value}</dd></div>}
