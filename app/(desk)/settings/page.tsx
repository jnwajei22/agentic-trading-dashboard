import Link from "next/link"
import { Server, ShieldCheck, UserRoundCog } from "lucide-react"
import { PageHeader } from "@/components/desk/page-header"
import { Card, CardContent } from "@/components/ui/card"

export const metadata = { title: "Settings" }
const sections = [{ href: "/settings/broker", icon: ShieldCheck, title: "Broker connections", detail: "Connect, refresh, reauthenticate, disable, or disconnect TradeLocker." }, { href: "/settings/accounts", icon: UserRoundCog, title: "Accounts", detail: "Manage aliases, default context, environment labels, and availability." }]
export default function SettingsPage() { return <div className="space-y-7"><PageHeader eyebrow="Workspace configuration" title="Settings" description="Manage safe application preferences and backend-owned broker context. Secrets and raw credentials are never displayed." /><div className="grid gap-4 md:grid-cols-2">{sections.map(({ href, icon: Icon, title, detail }) => <Link href={href} key={href}><Card className="h-full transition-colors hover:bg-accent"><CardContent className="flex gap-4 p-5"><span className="rounded-lg bg-muted p-3"><Icon className="h-5 w-5" /></span><div><h2 className="font-semibold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">{detail}</p></div></CardContent></Card></Link>)}<Card><CardContent className="flex gap-4 p-5"><span className="rounded-lg bg-muted p-3"><Server className="h-5 w-5" /></span><div><h2 className="font-semibold">Application</h2><p className="mt-1 text-sm text-muted-foreground">Theme follows the top navigation control. Runtime configuration is server-side and read-only.</p><dl className="mt-3 text-xs text-muted-foreground"><div className="flex gap-2"><dt>API boundary:</dt><dd>/api/afd/*</dd></div><div className="flex gap-2"><dt>Execution pricing:</dt><dd>Backend authoritative</dd></div></dl></div></CardContent></Card></div></div> }
